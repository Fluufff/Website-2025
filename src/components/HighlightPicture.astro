---
import { Picture } from 'astro:assets'
interface Props {
  image: ImageMetadata
  className?: string
}

const { image, className } = Astro.props
---

<Picture class={className} src={image} formats={['avif', 'webp']} fallbackFormat="jpg" alt="" widths={[400, 800]} />
