---
import AccordionItem from '../components/AccordionItem.astro'

interface Props {
  question: string
}

const { question } = Astro.props
---

<AccordionItem showClose>
  <Fragment slot="header">{question}</Fragment>
  <slot />
</AccordionItem>
